import React from 'react';
import './styles.scss';

const Header = () => {
  /* Eventually all text will be coming from text-constant file */
  return (
    <header className="header">
        <h1>News atOnce</h1>
    </header>
  );
}

export default Header;
